module.exports = (req, res) => {
    // receive user data
    // validate
    // write to db
    res.send({
        user_id: 'user_id',
        // Other meta data, probably wouldn't return anything else with this
    });
};
